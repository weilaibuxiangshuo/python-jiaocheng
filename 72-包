"""
思考:如果不同的人编写的模块同名怎么办？
解决：为了解决模块命名的冲突，引入了按目录组织模块的方法，称为包
特点：顶层的包不与其它发生冲突，模块都不会与其它包发生冲突
注意：目录只有包含一个叫做"__init__.py"的文件才被认作是一个包，
主要是为了避免一些滥竽充数的名字，基本上文件中什么也不写
"""

# 格式：import 路径.模块文件名
# 注意：每一个文件夹都需要一个__init__.py,模块文件，只需要模块名，不需要.py

# import a.sunck
# import b.sunck
# 不可以用from ...import...，否则引的函数重名就不行

import temp01.temp02.tt02
